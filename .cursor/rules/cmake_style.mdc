---
description: 
globs: CMakeLists.txt
alwaysApply: false
---
# ESP-IDF CMake 规则

## 基本规则
- 使用 4 个空格缩进
- 最大行长度 120 个字符
- 使用小写命令和函数名
- 使用有意义的变量名
- 保持 CMake 文件简洁

## 命名规则
### 变量
- 局部变量使用小写（带下划线）
- 全局变量使用大写（带下划线）
- 使用有意义的变量名
- 避免使用特殊字符

### 函数和宏
- 使用小写（带下划线）
- 函数名应清晰表达其功能
- 避免过长的函数名
- 使用有意义的参数名

## 文件组织
- 将相关功能组织在一起
- 使用注释分隔不同的部分
- 保持文件结构清晰
- 避免过长的文件

## 最佳实践
### 变量使用
- 使用 `set()` 设置变量
- 使用 `unset()` 清除变量
- 使用 `if()` 检查变量
- 使用 `message()` 输出信息

### 条件语句
- 使用 `if()` 进行条件判断
- 使用 `elseif()` 和 `else()` 处理其他情况
- 使用 `endif()` 结束条件块
- 保持条件语句简洁

### 循环
- 使用 `foreach()` 进行循环
- 使用 `while()` 进行条件循环
- 使用 `break()` 和 `continue()` 控制循环
- 避免过深的循环嵌套

### 函数定义
- 使用 `function()` 定义函数
- 使用 `endfunction()` 结束函数
- 使用 `return()` 返回结果
- 为函数添加文档注释

### 宏定义
- 使用 `macro()` 定义宏
- 使用 `endmacro()` 结束宏
- 使用 `return()` 返回结果
- 为宏添加文档注释

## 错误处理
- 使用 `message()` 输出错误信息
- 使用 `message(FATAL_ERROR ...)` 终止构建
- 使用 `message(WARNING ...)` 输出警告
- 使用 `message(STATUS ...)` 输出状态信息

## 模块化
- 将相关功能组织成模块
- 使用 `include()` 包含其他文件
- 使用 `add_subdirectory()` 添加子目录
- 保持模块之间的独立性

## 性能
- 避免不必要的变量设置
- 避免不必要的函数调用
- 使用缓存变量提高性能
- 避免过深的目录结构

## 可维护性
- 使用注释说明复杂逻辑
- 使用有意义的变量名
- 保持代码结构清晰
- 避免重复代码

## 调试
- 使用 `message()` 输出调试信息
- 使用 `message(DEBUG ...)` 输出详细调试信息
- 使用 `message(VERBOSE ...)` 输出详细信息
- 使用 `message(TRACE ...)` 输出跟踪信息
