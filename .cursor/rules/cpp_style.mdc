---
description: 
globs: *.cpp
alwaysApply: false
---
# ESP-IDF C++ 语言规则

## 文件命名
- C++ 头文件使用 `.hpp` 扩展名
- C++ 源文件使用 `.cpp` 扩展名
- 文件名应反映其内容
- 避免使用特殊字符

## 命名规则
### 类名
- 使用 `CamelCase`
- 类名应清晰表达其用途
- 避免使用缩写
- 接口类不应使用 `Interface` 后缀

### 成员变量
- 使用 `snake_case`
- 私有成员变量可以添加 `m_` 前缀
- 静态成员变量可以添加 `s_` 前缀
- 常量成员变量使用全大写

### 方法
- 使用 `snake_case`
- 方法名应清晰表达其功能
- 获取方法使用 `get_` 前缀
- 设置方法使用 `set_` 前缀
- 布尔方法使用 `is_`、`has_` 等前缀

### 命名空间
- 使用小写 `snake_case`
- 避免过深的命名空间嵌套
- 使用有意义的命名空间名

## 类设计
### 成员顺序
1. 公共成员
2. 受保护成员
3. 私有成员
4. 构造函数/析构函数
5. 成员函数
6. 成员变量

### 构造函数
- 使用初始化列表
- 避免在构造函数中调用虚函数
- 构造函数应初始化所有成员变量
- 使用 `explicit` 关键字防止隐式转换

### 析构函数
- 虚析构函数用于基类
- 确保正确释放资源
- 避免在析构函数中抛出异常

## 模板
- 模板声明放在函数声明上方
- 使用有意义的模板参数名
- 避免过度使用模板
- 为模板提供适当的文档

## 异常处理
- 使用 ESP-IDF 错误处理机制
- 避免使用异常处理
- 如果必须使用异常，确保正确捕获
- 不要在析构函数中抛出异常

## 智能指针
- 优先使用 `std::unique_ptr`
- 谨慎使用 `std::shared_ptr`
- 避免使用原始指针
- 使用 `make_unique` 和 `make_shared`

## 现代 C++ 特性
- 使用 `auto` 关键字
- 使用 `nullptr` 代替 `NULL`
- 使用 `override` 关键字
- 使用 `final` 关键字
- 使用 `constexpr` 和 `const`
- 使用 `enum class`

## 代码格式
### 缩进
- 使用 4 个空格
- 不要在命名空间内缩进
- 类成员缩进 4 个空格

### 大括号
- 类定义的大括号单独占一行
- 方法定义的大括号单独占一行
- 其他代码块的大括号与语句同行

### 空格
- 在运算符周围添加空格
- 在逗号后添加空格
- 在分号后添加空格
- 在冒号后添加空格

## 注释
- 使用 `//` 进行单行注释
- 为类和方法添加文档注释
- 为复杂的算法添加注释
- 使用 TODO、FIXME 等标记时添加说明

## 性能
- 避免不必要的对象复制
- 使用移动语义
- 使用引用避免复制
- 使用 `const` 引用参数

## 线程安全
- 注意多线程访问的同步
- 使用适当的锁机制
- 避免死锁
- 使用原子操作
