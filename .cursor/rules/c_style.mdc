---
description: 
globs: *.c,*.h
alwaysApply: false
---
# ESP-IDF C 语言规则

## 命名规则
- 仅在单个源文件中使用的变量或函数应声明为 `static`
- 公共名称（非静态变量和函数）应使用组件或单元前缀命名空间，例如 `esp_vfs_register()`
- 静态变量应使用 `s_` 前缀，例如 `static bool s_invert`
- 避免不必要的缩写
- 使用有意义的变量名和函数名

## 代码格式
### 缩进
- 使用 4 个空格进行缩进
- 不要使用制表符
- 配置编辑器在按 Tab 键时输出 4 个空格

### 垂直空间
- 函数之间放置一个空行
- 不要在函数开始或结束时放置空行
- 最大行长度为 120 个字符
- 相关代码块之间使用空行分隔

### 水平空间
- 在条件语句和循环关键字后添加单个空格
- 在二元运算符周围添加单个空格
- 一元运算符不需要空格
- 在 `.` 和 `->` 运算符周围不需要空格
- 不要在行尾添加尾随空格
- 函数参数列表中的逗号后添加空格

### 大括号
- 函数定义的大括号应单独占一行
- 在函数内部，条件语句和循环语句的左大括号应与语句在同一行
- 单行语句块可以使用大括号，也可以不使用
- 多行语句块必须使用大括号

## 注释规范
- 注释使用英文
- 使用 `//` 进行单行注释
- 多行注释可以使用每行 `//` 或 `/* */` 块
- 不要使用注释来禁用功能
- 如果代码不再需要，应完全删除
- 不要添加关于作者身份和更改日期的琐碎注释
- 为复杂的算法和逻辑添加必要的注释
- 使用 TODO、FIXME 等标记时添加说明

## 类型定义
- 使用 `snake_case`，以 `_t` 结尾
- 枚举应通过 `typedef` 定义并使用命名空间
- 结构体定义应使用 `typedef`
- 避免使用基本类型，使用 ESP-IDF 定义的类型

## 头文件
### 保护
- 所有公共头文件应使用预处理器保护
- 优先使用 `#pragma once`
- 所有 C 头文件应有 `extern "C"` 保护

### 包含顺序
1. C 标准库头文件
2. POSIX 标准头文件
3. 通用 IDF 头文件
4. 其他组件的头文件
5. 当前组件的公共头文件
6. 私有头文件

## 函数
- 函数应简短且专注于单一任务
- 函数参数不应超过 5 个
- 使用有意义的函数名
- 函数应返回错误码或状态
- 避免使用全局变量

## 错误处理
- 使用 ESP-IDF 错误处理机制
- 检查所有可能失败的操作
- 提供有意义的错误信息
- 使用断言检查不可恢复的错误

## 内存管理
- 使用 ESP-IDF 内存管理函数
- 避免内存泄漏
- 检查内存分配是否成功
- 使用适当的内存池

## 线程安全
- 注意多线程访问的同步
- 使用适当的锁机制
- 避免死锁
- 使用原子操作
